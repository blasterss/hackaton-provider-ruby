# frozen_string_literal: true

class Provider
  class <%= integration.provider.class_name %>Service < BaseService
    BASE_URL = ENV.fetch('<%= integration.provider.base_url_env_name %>', '<%= integration.provider.base_url %>')

<%= render_blocks(:constants) -%>
<%= render_blocks(:public_methods) -%>

    private

<%= render_blocks(:private_methods) -%>
  end
end
